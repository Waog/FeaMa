sudo: false
language: node_js
node_js:
  - "0.12"
before_install:
  - npm install -g grunt-cli
  - npm install -g tsd
  - npm install -g bower
  - echo $NODE_PATH
cache:
  directories:
    - bower_components
    - node_modules
    - typings
    